Uncertainty qualification of atomic mobility parameters
##############################################################

MCMC simuation
----------------
Compile the `src/mcmc/mcmc.cpp` by running `make` and visualize the result file with the python script `showseq.py`.